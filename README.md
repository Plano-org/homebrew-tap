# Plano-org Tap

## How do I install these formulae?

`brew install plano-org/tap/<formula>`

Or `brew tap plano-org/tap` and then `brew install <formula>`.

Or, in a `brew bundle` `Brewfile`:

```ruby
tap "plano-org/tap"
brew "<formula>"
```

## Documentation

`brew help`, `man brew` or check [Homebrew's documentation](https://docs.brew.sh).
